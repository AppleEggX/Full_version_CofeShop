@model IEnumerable<CofeWebApplication.ServiceReference1.BasketItemDTO>

@{
    ViewBag.Title = "Index";
}


    <div class=" container" style="padding-bottom:300px">
        <h2>Index</h2>

        <p>
            @Html.ActionLink("Create New", "Create")
        </p>
        <table class="table">
            <tr>
                <th>
                    @*@Html.DisplayNameFor(model => model.Quantity)*@
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.TotalPrice)
                </th>
                @*<th>
                    @Html.DisplayNameFor(model => model.Basket.Id)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Coffee)*@
                </th>
                <th></th>
            </tr>

            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Quantity)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.TotalPrice)
                    </td>
                    @*<td>
                        @Html.DisplayFor(modelItem => item.Basket.Id)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Coffee.Name)
                    </td>
                    <td>
                        @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                        @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                        @Html.ActionLink("Delete", "Delete", new { id = item.Id })
                    </td>*@
                </tr>
            }

        </table>
    </div>